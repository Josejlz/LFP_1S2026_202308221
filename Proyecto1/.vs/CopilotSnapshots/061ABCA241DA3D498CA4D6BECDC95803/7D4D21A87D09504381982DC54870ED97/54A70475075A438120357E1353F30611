#include "LexicalAnalyzer.h"
#include <wx/log.h>
#include <wx/string.h>
#include <string>
#include <cctype>
#include <iostream>
#include <fstream>
#include <stdexcept>
#include <sstream>
#include <vector>


LexicalAnalyzer::LexicalAnalyzer() {
	pos = 0;
	line = 1;
	col = 1;
}

static TokenType clasificacionPalabra(const std::string& p) {
	if (p=="HOSPITAL"||p=="PACIENTES"||p=="MEDICOS"||p=="CITAS" || p =="DIAGNOSTICOS") {
		return RESERVADA;
	}
	if (p=="CARDIOLOGIA"||p=="NEUROLOGIA"||p=="PEDIATRIA"||p=="CIRUGIA"||p=="MEDICINA_GENERAL"||p=="ONCOLOGIA"||p=="PSICOLOGIA"||p=="DIARIA"||p=="CADA_8_HORAS" || p == "CADA_12_HORAS" || p == "SEMANAL") {
		return ENUM_VAL;
	}

	return IDENTIFICADOR;
		
}


void LexicalAnalyzer::nextToken(wxString filepath) {

	std::string filename = filepath.ToStdString();
	std::string lexeme = "";
	int state = 0;
	int startCol = col;
	int startLine = line;

	while (true) {

	}

	



}